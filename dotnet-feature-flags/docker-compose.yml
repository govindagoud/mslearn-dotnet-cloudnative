version: '3.4'

services:

  frontend:
    image: storeimage
    build:
      context: .
      dockerfile: DockerfileStore
    environment:
      - ProductEndpoint=http://backend:8080
      - ImagePrefix=http://localhost:32001/images
      - ConnectionStrings:AppConfig=Endpoint=https://eshop-app-features1743105759.azconfig.io;Id=5PZD;Secret=2oaiIm0/qBmdiy+tvDzC49x14cZz1DP4ZrYrXysHR/s=
    ports:
      - "32000:8080"
    depends_on:
      - backend
  backend:
    image: productservice
    build:
      context: .
      dockerfile: DockerfileProducts
    ports:
      - "32001:8080"
